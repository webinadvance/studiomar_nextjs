using Bridge;
namespace Model.Enums
{
    [Enum(Emit.StringNameUpperCase)]
    public enum EACTION
    {
        DEPOSIT,
        BALANCE,
        ASSOCIATION,
        CREDITS_ADD,
        CREDITS_REM,
    }
}
